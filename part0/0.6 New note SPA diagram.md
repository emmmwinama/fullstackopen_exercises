sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a new note and clicks the Save button. The JavaScript running in the browser captures the note and updates the DOM immediately (without reloading the page)

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: HTTP 201 Status
    deactivate server

    Note right of browser: The browser adds the new note to the existing list of notes on the page dynamically, without fetching the HTML or reloading the page
